逻辑回归（Logistic Regression）模型的预测与训练
一、预测过程
概率计算：用 Sigmoid 函数算正类概率
$p^​=σ(θTx)$ ，其中 $σ(t)=1+e−t1$​（把实数映射到 (0,1) ）
类别判断：按概率阈值（通常 0.5）分类
y^​={01​p^​<0.5p^​≥0.5​

二、训练过程
目标：优化参数 θ ，最小化损失函数

单样本损失：惩罚预测与真实标签的偏差
c(θ)={−log(p^​)−log(1−p^​)​y=1y=0​

整体损失（训练集）：平均单样本损失，凸函数保证梯度下降找全局最优
J(θ)=−m1​∑i=1m​[y(i)log(p^​(i))+(1−y(i))log(1−p^​(i))]

梯度更新：用梯度下降优化参数，参数 θj​ 的更新依据
∂θj​∂J(θ)​=m1​∑i=1m​(σ(θTx(i))−y(i))xj(i)​

核心逻辑：预测靠 Sigmoid 算概率、判类别；训练通过定义损失，用梯度下降找最优参数 。